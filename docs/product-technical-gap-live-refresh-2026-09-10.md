# Product and technical gap live refresh — 2026-09-10

Status: **Non-authoritative live supplement**  
Protected-product authority: `main@493326f2de49ea1704da0ded19868ed05d2fe00f`  
Central workflow authority: `ContextualWisdomLab/.github@cb0872c9a20d5584703dffacca65c096fc034c6c`  
Canonical historical baseline: `docs/product-technical-gap-baseline.md`  
Latest immutable release: `v0.9.1` (published 2026-08-26, immutable)

This supplement updates transient execution state without rewriting the evidence-rich historical baseline. Older live-refresh statements remain historical evidence; the exact states below supersede them for current execution decisions. A capability is shipped only after protected integration and its exact-head scientific, package, coverage, security, review, provenance, and release gates are satisfied. Drafts, queued checks, predecessor approvals, and foreign-owner PR heads remain evidence inputs only.

`fast-mlsirm` remains the canonical owner for reusable LSIRM/MLSIRM/IRT/generalized-dependence numerical kernels, true-parameter recovery, and stable public bindings. TEPP owns temporal/event composition. `contextual-orchestrator` owns provider/model routing and LLM orchestration. Foreign-owner source or mutable PR heads are not copied into this repository.

## Live high-leverage lanes

| Gap | Exact owner state | Current exact evidence | Required next evidence |
| --- | --- | --- | --- |
| Rust 1.98.1 compiler/recovery qualification | #1773 `e49e631fbd672b27d408854d6c2409eea9ae073d`, Ready | Statistical Studies `34426709158` is terminal GREEN across all 25 governed jobs. Ordinary CI `34426709224`, ClusterFuzzLite `34426709173`, Security Scan `34426709276`, repository CodeQL `34426709299`, and Semgrep `34426709197` are GREEN. Required CodeQL PR `34426709242` is RED in the central control-plane lane. | Keep the 25/25 scientific result as current-head evidence, not shipped status. Normal landing still requires the central required-workflow defect to be repaired on its canonical owner path and a qualifying independent current-head approval. |
| Person-fit primary-source traceability | #1791 `bfe9c22ac4355da45504211148bec914bc632909`, Ready | Docs-only delta. Current-head CI `34321778532`, Required CodeQL PR `34309381421`, Security Scan `34309381490`, repository CodeQL `34309381389`, and Semgrep `34309381229` are GREEN. No qualifying current-head `APPROVED` review is established. | Preserve `PRIMARY_READ_EQUATION` as stricter than definition, metadata, bridge, or package-comparator evidence. Keep #1790 open until the implementation-relevant primary equations/conventions and executable seven-statistic conformance evidence are completed. |
| Raw JSON depth-floor security boundary | #1738 `4e1f8ade9a5ae90cf3e9eb4ccd8feb8a89023080`, Draft | The repaired product tree is unchanged while repeated source-neutral commits have continued even after Draft containment. Predecessor CI/review evidence is therefore not transferred. | Stop or demonstrate quiescence of the competing source writer, then obtain all ordinary/security/central/review evidence on one unchanged SHA before Ready admission. Do not create another history-only retrigger commit. |
| Strict JSON duplicate/non-finite admission | #1744 `48756e76c092237c40e59e613c33debb9fa877df`, Draft | Valid parser intent remains, but the focused `tests/test_strict_json_deserialization.py` evidence is absent from the current tree; generation does not independently pin `NaN`, `Infinity`, and `-Infinity`; new strict-decoding helpers lack boundary-purpose docstrings; Sentinel provenance again overstates the protected-main duplicate-key defect. | Wait for #1738 protected integration, then non-force reconcile from protected main. Restore focused hostile RED→GREEN coverage, accurate provenance, helper docstrings, and exact-head hosted/review evidence without copying a mutable prerequisite head. |
| Rust release-build warning in `lsr_pass` | #1799 open; reserved branch `fix/lsr-unused-nf-20260910@653ec84d443e6c9914aef8be3b53be9c32f2b299` | Protected main still contains one unused `let nf = n as f64;` at the `lsr_pass` boundary. Earlier broad full-file mutation was repaired forward and has zero effective product delta. | Delete only the dead binding through a safe complete-source/Git-data path, verify protected-main diff is exactly the causal production deletion, then run workspace/all-target Rust checks and scaling regressions. No lint suppression or warning-denominator change. |
| LLM credential/routing owner boundary | #1800 open | Protected documentation still describes direct provider-key ownership, but `contextual-orchestrator` has no immutable released API/client/schema authority suitable for a consumer pin. | Keep leaf migration fail-closed until contextual-orchestrator publishes a versioned immutable contract. Then consume only gateway-token + `orchestrator/free`; do not copy provider discovery, model/group selection, paid fallback, or routing source into fast-mlsirm. |
| Report keyboard focus / bypass interaction | #1768 `e70e7739114092eaf101bbaf070c2d58b939484e`, Draft | Canonical report lane has exact-head real-browser keyboard/pointer/responsive evidence and leaf security checks; dependent #1804/#1796 remain non-landing candidates. | Preserve unique dependent deltas only through the canonical lane. Central Required CodeQL and live review requirements must settle before normal landing; static CSS presence is not substituted for browser interaction evidence. |
| Marginal/GPCM/rotation performance candidates | #1742 / #1736 canonical numerical owners; #1798/#1801/#1805 Draft candidates | Proposed NumPy/einsum/fold substitutions can alter binary64 reduction identity and currently lack governed benchmark/profile, allocation/RSS, true-parameter recovery, or buyer-path latency evidence. | Profile representative workloads first. Any material hot path remains Rust-first and must preserve deterministic CPU-f64 reference/parity or record an explicit reviewed numerical-contract change. Unmeasured speedup claims are not product evidence. |
| Central Required CodeQL rollout | `.github#2051@558693e0333e48012beea142f739bc634b0674a7`, Draft; #2056 stacked Draft | Protected central authority is `.github/main@cb0872c9a20d5584703dffacca65c096fc034c6c`; #2051 still records an older base and owns the versioned handler/bootstrap and exact receipt/wake repair. Multiple fast-mlsirm consumer heads reproduce the ordering failure while their leaf CodeQL/security evidence is GREEN. | Reconcile the central prerequisite non-destructively onto current protected authority, land the versioned backward-compatible handler prerequisite first, then obtain a fresh unchanged-head consumer generation. Do not synthesize success or copy dispatch/status logic into the leaf. |

## Current commercial/scientific admission

Open inventory at this refresh is 83 pull requests and 204 issues. The repository-wide open-PR search contains zero `review:approved` results. These counts are workload inventory, not quality or completion metrics.

The immediate highest-value path is to convert already-complete exact-head evidence into protected product authority without weakening governance: unblock the central Required CodeQL owner path and obtain independent current-head approvals for otherwise-clean candidates such as #1791 and, after central repair, #1773. In parallel, keep source-changing scientific work on its existing numerical owners: person-fit primary-equation/conformance closure, the one-line Rust release-warning repair, strict JSON repair after the depth-floor prerequisite, and measured Rust-first optimization only where profiling shows a material buyer path.

No source-neutral retrigger, sample reduction, tolerance relaxation, skipped/xfail acceptance, self-approval, force update, destructive rebase, foreign-owner source copy, or predecessor-success transfer is accepted as completion evidence.
