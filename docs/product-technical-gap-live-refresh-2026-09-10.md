# Product and technical gap live refresh — 2026-09-10

Status: **Non-authoritative live supplement**  
Protected-product authority: `main@493326f2de49ea1704da0ded19868ed05d2fe00f`  
Central workflow authority: `ContextualWisdomLab/.github@cb0872c9a20d5584703dffacca65c096fc034c6c`  
Canonical historical baseline: `docs/product-technical-gap-baseline.md`  
Latest immutable release: `v0.9.1` (published 2026-08-26, immutable)

This supplement updates transient execution state without rewriting the evidence-rich historical baseline. Older live-refresh statements remain historical evidence; the exact states below supersede them for current execution decisions. A capability is shipped only after protected integration and its exact-head scientific, package, coverage, security, review, provenance, and release gates are satisfied. Drafts, queued checks, predecessor approvals, and foreign-owner PR heads remain evidence inputs only.

`fast-mlsirm` remains the canonical owner for reusable LSIRM/MLSIRM/IRT/generalized-dependence numerical kernels, true-parameter recovery, and stable public bindings. TEPP owns temporal/event composition. `contextual-orchestrator` owns provider/model routing and LLM orchestration. Foreign-owner source or mutable PR heads are not copied into this repository.

## Live high-leverage lanes

| Gap | Exact owner state | Current exact evidence | Required next evidence |
| --- | --- | --- | --- |
| Rust 1.98.1 compiler/recovery qualification | #1773 `e49e631fbd672b27d408854d6c2409eea9ae073d`, Ready | Statistical Studies `34426709158` is terminal GREEN across all 25 governed jobs. Ordinary CI `34426709224`, ClusterFuzzLite `34426709173`, Security Scan `34426709276`, repository CodeQL `34426709299`, and Semgrep `34426709197` are GREEN. Required CodeQL PR `34426709242` is RED in the central control-plane lane. | Keep the 25/25 scientific result as current-head evidence, not shipped status. Normal landing still requires the central required-workflow defect to be repaired on its canonical owner path and a qualifying independent current-head approval. |
| Person-fit primary-source traceability | #1791 `bfe9c22ac4355da45504211148bec914bc632909`, Ready | Docs-only delta. Current-head CI `34321778532`, Required CodeQL PR `34309381421`, Security Scan `34309381490`, repository CodeQL `34309381389`, and Semgrep `34309381229` are GREEN. No qualifying current-head `APPROVED` review is established. | Preserve `PRIMARY_READ_EQUATION` as stricter than definition, metadata, bridge, or package-comparator evidence. Keep #1790 open until the implementation-relevant primary equations/conventions and executable seven-statistic conformance evidence are completed. |
| Raw JSON depth-floor security boundary | #1738 `4e1f8ade9a5ae90cf3e9eb4ccd8feb8a89023080`, Draft | The repaired product tree is unchanged while repeated source-neutral commits have continued even after Draft containment. Predecessor CI/review evidence is therefore not transferred. | Stop or demonstrate quiescence of the competing source writer, then obtain all ordinary/security/central/review evidence on one unchanged SHA before Ready admission. Do not create another history-only retrigger commit. |
| Strict JSON duplicate/non-finite admission | #1744 `48756e76c092237c40e59e613c33debb9fa877df`, Draft | Valid parser intent remains, but the focused `tests/test_strict_json_deserialization.py` evidence is absent from the current tree; generation does not independently pin `NaN`, `Infinity`, and `-Infinity`; new strict-decoding helpers lack boundary-purpose docstrings; Sentinel provenance again overstates the protected-main duplicate-key defect. | Wait for #1738 protected integration, then non-force reconcile from protected main. Restore focused hostile RED→GREEN coverage, accurate provenance, helper docstrings, and exact-head hosted/review evidence without copying a mutable prerequisite head. |
| Rust release-build warning in `lsr_pass` | #1799 open; reserved branch `fix/lsr-unused-nf-20260910@653ec84d443e6c9914aef8be3b53be9c32f2b299` | Protected main still contains one unused `let nf = n as f64;` at the `lsr_pass` boundary. Earlier broad full-file mutation was repaired forward and has zero effective product delta. | Delete only the dead binding through a safe complete-source/Git-data path, verify protected-main diff is exactly the causal production deletion, then run workspace/all-target Rust checks and scaling regressions. No lint suppression or warning-denominator change. |
| LLM credential/routing owner boundary | #1800 open | Protected documentation still describes direct provider-key ownership, but `contextual-orchestrator` has no immutable released API/client/schema authority suitable for a consumer pin. | Keep leaf migration fail-closed until contextual-orchestrator publishes a versioned immutable contract. Then consume only gateway-token + `orchestrator/free`; do not copy provider discovery, model/group selection, paid fallback, or routing source into fast-mlsirm. |
| Report keyboard focus / bypass interaction | #1768 `e70e7739114092eaf101bbaf070c2d58b939484e`, Draft | Canonical report lane has exact-head real-browser keyboard/pointer/responsive evidence and leaf security checks; dependent #1804/#1796 remain non-landing candidates. | Preserve unique dependent deltas only through the canonical lane. Central Required CodeQL and live review requirements must settle before normal landing; static CSS presence is not substituted for browser interaction evidence. |
| Marginal binary64 contract and optimization admission | #1742 `785e98c9282a51f77559e83133f5d8da16ad5942`, Draft; rejected competing #1805 closed with verified no-valid-delta | Effective protected-main product delta is exactly two regression files. The exact hexadecimal one-ULP fixture now drives the real `fit_marginal_numpy` M-step and proves that reassociating only the live row-wise squared-distance reduction changes returned zeta binary64 bits; the prior source-text assertion gap is repaired. Repository CodeQL `34507631930`, Security `34507631954`, and Semgrep `34507631933` are GREEN. Required CodeQL PR `34507631968` is RED in the central owner lane. | Keep production arithmetic unchanged. Do not re-admit `einsum`/`vdot`/fold optimization without governed profile, allocation/RSS, deterministic CPU-f64 parity and realistic recovery evidence. Keep this lane Draft until central `.github#2082` stops stale cancellation/invalid zero-diff closure and `.github#2051` repairs required-CodeQL lifecycle ordering; then obtain one stable unchanged-head Ready generation. |
| Central Required CodeQL rollout | `.github#2051@558693e0333e48012beea142f739bc634b0674a7`, Draft; #2056 stacked Draft | Protected central authority is `.github/main@cb0872c9a20d5584703dffacca65c096fc034c6c`; #2051 still records an older base and owns the versioned handler/bootstrap and exact receipt/wake repair. On #1742 current head, Required CodeQL PR `34507631968` again let actions/python receivers finish reading their current-head verdicts and fail enforcement before coordinator `102974614561` subsequently dispatched the generation. | Reconcile the central prerequisite non-destructively onto current protected authority, land the versioned backward-compatible handler prerequisite first, then obtain a fresh unchanged-head consumer generation whose dispatch/receipt authority exists before receiver enforcement. Do not synthesize success or copy dispatch/status logic into the leaf. |
| PR writer/closure lineage safety | central `.github#2082`, open; fast-mlsirm #1742 is the live canary | A stale Jules cancellation removed #1742's two still-valid regression files twice after Ready admission. OpenCode then twice closed the instantaneous zero-diff PR despite no verified successor inheriting the scientific delta. Both losses were repaired by ordinary forward commits; `3830907f7c1e7bdfb3b6efc0b0cf582430bb5148...785e98c9282a51f77559e83133f5d8da16ad5942` has `files=[]`, proving byte-for-byte restoration of the reviewed behavioral tree. | Auto-close admission must use lineage/successor authority, not terminal `diff==0` alone, and stale source writers must adopt the newer canonical state instead of replaying obsolete cancellation intent. #1742-class disappearing-valid-delta stays open/Draft; #1805-class affirmatively rejected/no-unique-delta may close. |

## Current commercial/scientific admission

Open inventory at this refresh is 82 pull requests and 204 issues. The repository-wide open-PR search contains zero `review:approved` results. These counts are workload inventory, not quality or completion metrics.

The immediate highest-value path is to convert already-complete exact-head evidence into protected product authority without weakening governance: unblock the central Required CodeQL owner path and obtain independent current-head approvals for otherwise-clean candidates such as #1791 and, after central repair, #1773. In parallel, keep source-changing scientific work on its existing numerical owners: person-fit primary-equation/conformance closure, the one-line Rust release-warning repair, strict JSON repair after the depth-floor prerequisite, and measured Rust-first optimization only where profiling shows a material buyer path.

No source-neutral retrigger, sample reduction, tolerance relaxation, skipped/xfail acceptance, self-approval, force update, destructive rebase, foreign-owner source copy, or predecessor-success transfer is accepted as completion evidence.

## Review-gateway causal update — 2026-09-10

The current #1519 review cycle separates leaf quality from foreign review admission more precisely. On exact head `84000d861ddc9b8cc72a39e7c78f8016051e0e6c`, ordinary CI, Required CodeQL PR, repository CodeQL, Security Scan, and Semgrep are terminal GREEN, and all inline review threads are resolved or outdated. Two `CHANGES_REQUESTED` submissions tied only to predecessor-head Noema failures were dismissed as stale; no predecessor review was transferred as an approval.

Current-head Required Noema run `34469361714`, job `102845420373`, failed outside the docs/source lane. Trusted workflow source `.github@cb0872c9a20d5584703dffacca65c096fc034c6c` vendored `contextual-orchestrator@414f22973658c4ddc3d4320fcf7acd9b4e8ba991`. Its preflight reached a ready state, but the single caller request to `orchestrator/free` failed after 182.4 seconds with HTTP 429 during `response_error`, with served model `google/gemma-4-31b-it:free`; no source finding or authenticated verdict envelope was produced. Current-head OpenCode also has no authenticated exact-head verdict, so its required job fails closed rather than supplying independent source evidence.

This is now an explicit owner/consumer version gap. `contextual-orchestrator#1094` merged on 2026-09-09 with virtual `orchestrator/free` structured-output re-selection for retryable 429/502/timeout outcomes, and protected contextual-orchestrator main has advanced beyond that integration, but the central consumer still pins the earlier `414f2297…` commit. contextual-orchestrator currently has no immutable GitHub Release, so pinning mutable main or copying the newer routing implementation into `.github` or fast-mlsirm would violate the released-owner boundary. Exact evidence was handed to `contextual-orchestrator#1106` and `.github#1759`; acceptance remains owner RED→integrated GREEN→immutable owner release→central consumer bump→unchanged leaf-head review GREEN.

This failure is not universal evidence that the #1791 source is defective or that the review service can never succeed. #1791 exact head `bfe9c22ac4355da45504211148bec914bc632909` has terminal-success `noema-review` job `102333782769` and `opencode-review` job `102334444517`, in addition to its other hosted gates. It still lacks a qualifying formal current-head `APPROVED` review, so the successful checks are review-system evidence rather than merge authorization.

## Marginal writer-lineage causal update — 2026-09-11

#1742 exposed two distinct central lifecycle defects without changing production arithmetic. The scientific-test gap is now repaired: the test still pins the exact finite binary64 counterexample, but it no longer treats source spelling as acceptance. It executes the real estimator M-step and demonstrates a returned-state bit change under the rejected reassociation.

The valid tree was then erased twice by the same obsolete cancellation intent after Ready admission. Each transient zero-diff snapshot was auto-closed before any verified successor inherited the regression contract. The lane was recovered with forward-only commits and deliberately returned to Draft. Central `.github#2082` now owns lineage-aware close admission and stale-writer containment; fast-mlsirm must not copy scheduler/closer logic into the numerical engine.

Current Required CodeQL run `34507631968` supplies an independent exact-head control-plane canary: both compatibility receivers failed enforcement only after successfully waiting for a current-head dispatch verdict, and the coordinator dispatched afterward. This evidence was handed to `.github#2051`. Leaf repository CodeQL, Security and Semgrep are GREEN on the same head, so another leaf no-op, threshold change, or numerical workaround is not causal repair.
