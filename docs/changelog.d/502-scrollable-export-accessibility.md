# Keyboard-accessible exact-value exports

## Changed

- Made JSON and CSV exact-value export regions keyboard-focusable and explicitly named for assistive technology, so keyboard-only readers can horizontally scroll wide report content.
- Added visible focus treatment for export code regions and lightweight hover feedback for disclosure controls while preserving the report's reduced-motion behavior.
