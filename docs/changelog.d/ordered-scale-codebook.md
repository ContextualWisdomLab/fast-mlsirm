# Codebook-anchored ordered response preparation

## Added

- Add explicit, versioned scale-codebook response preparation with authoritative
  item order, raw-category order, preserved missing values, immutable output,
  and a reproducible key fingerprint. No keys are inferred from response
  correlations and no IRT numerical kernel or scoring formula changes.
- Add data-free unit contracts and fixed-seed Rust GRM recovery/direction
  acceptance, plus a bounded installed-wheel evidence workflow. The workflow
  archives only synthetic-test evidence and the native package, not study data.
