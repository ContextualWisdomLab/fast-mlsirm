# Keyboard-accessible report export regions

## Fixed

- Generated diagnostics reports now expose horizontally scrollable JSON and CSV export blocks as named, keyboard-focusable regions with a visible focus indicator, so keyboard-only users can inspect full-precision export content without pointer input.
