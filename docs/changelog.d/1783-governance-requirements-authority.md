# Canonical requirements governance links

## Fixed

- Point the governance index at the canonical `docs/PRD.md` and `docs/TRD.md` requirements instead of the deprecated historical `docs/prd_trd_summary.md`, with a regression contract preventing the stale authority from returning.
