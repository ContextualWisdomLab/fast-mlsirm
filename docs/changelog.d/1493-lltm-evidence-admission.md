# LLTM scientific-evidence admission

## Fixed

- Seal the persons-by-items response matrix and items-by-basic-operations LLTM design before caller NumPy array or nested numeric conversion protocols can execute.
- Preserve exact real-numeric NumPy matrices, ordinary exact built-in rectangular matrices, exact numeric NumPy rows/scalars, the existing complex-specific diagnostics, and control-before-data admission ordering.
- Treat only `NaN` as public LLTM response missingness, reject response infinities before compiled-core discovery, and replay the Rust finite-`q_design` contract at the Python boundary.
- Bound each LLTM scientific matrix to 20,000,000 logical cells before dense `float64` marshalling, including exact broadcast NumPy views and built-in rectangular carriers.
- Keep Fischer LLTM likelihood, marginal-ML EM, likelihood-ratio testing, scoring, and uncertainty arithmetic in the Rust numerical core; this change is limited to Python validation and marshalling.
