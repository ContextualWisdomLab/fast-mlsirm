### Security

- Harden the generated-item testlet pilot handoff so model and execution controls establish exact trusted built-in or supported NumPy scalar identities before normalization or conversion. Caller-defined protocol, numeric, and string subclasses are rejected without callback dispatch while existing limits and Rust-owned testlet arithmetic remain unchanged.
