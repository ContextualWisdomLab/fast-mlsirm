# Residual interaction-map identifier budget

## Fixed

- Bound exact built-in person/item identifier carriers before copying them into the versioned interaction-map envelope. Identifier counts now fail closed above the existing 20,000,000-cell evidence envelope before matrix admission or compiled-core discovery, while valid opaque identifiers and all Rust-owned Gabriel map arithmetic remain unchanged.
