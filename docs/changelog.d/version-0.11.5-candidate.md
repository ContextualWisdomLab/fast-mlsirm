# Version 0.11.5 candidate

## Changed

- Project version is bumped to 0.11.5 in `pyproject.toml`, `crates/mlsirm-core`,
  `crates/fast-mlsirm-py`, both Cargo lockfiles, and `uv.lock`, because 0.11.4 is
  already published on PyPI (tag `v0.11.4`, 2026-09-18) and cannot be republished.
- Not a release cut: the `CHANGELOG.md` section, tag, and release notes follow only
  after the A3 release gates pass.
