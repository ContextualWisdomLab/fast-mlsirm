# Cross-engine conformance provenance manifest

## Added

- Add a source-free, content-addressed manifest contract for capability scope,
  comparison layer, independent-engine identity, parameter mapping, tolerance,
  code/fixture/environment provenance, RNG seeds, and license classification;
  external engines and numerical comparison arithmetic remain optional,
  isolated, and outside the production runtime dependency graph.
