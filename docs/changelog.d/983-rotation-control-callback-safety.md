# Harden factor-rotation semantic-control boundaries

## Fixed

- Reject caller-defined text, Boolean, integer, and real conversion providers before rotation-core discovery across direct rotation, criterion-gradient, and criterion-selection APIs, while preserving exact built-in and supported concrete NumPy scalar compatibility and leaving all rotation/selection arithmetic Rust-owned.
