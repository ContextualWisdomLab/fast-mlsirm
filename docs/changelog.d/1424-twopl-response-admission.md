- Compensatory 2PL response admission now rejects caller-controlled array/numeric conversion protocols, complex evidence, ragged/non-numeric matrices, and wider-precision values that would only become valid dichotomous observations after binary64 narrowing. Exact NumPy numeric matrices and ordinary built-in matrices of concrete Python/NumPy 0/1/NaN values remain supported; accepted evidence is normalized to package-owned contiguous `float64` before the existing Rust estimator boundary. The 2PL likelihood, quadrature, latent-correlation ECM, convergence, and scoring arithmetic are unchanged and remain Rust-owned.