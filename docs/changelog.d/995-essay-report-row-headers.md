# Improve essay report table row headers

## Fixed

- Mark the first identity or ordered-axis column in essay calibration and validation HTML tables as semantic row headers so assistive technologies can associate each data cell with its row while preserving the existing visual layout.
