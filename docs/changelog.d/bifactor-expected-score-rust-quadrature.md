- Route bifactor expected-total prediction through the shared Rust GRM and
  Gauss-Hermite kernels, and reject malformed, non-finite, negative-weight, or
  zero-mass quadrature rules before reporting an expected-score curve.
