diff --git a/.jules/bolt.md b/.jules/bolt.md
--- a/.jules/bolt.md
+++ b/.jules/bolt.md
@@ -44,6 +44,3 @@
 ## 2026-08-04 - Matrix-vector reductions for MMLE quadrature nodes
 **Learning:** In the NumPy MMLE reference fallback, expressions such as `(resid * nodes[None, :]).sum(axis=1)` materialize an item-by-node intermediate array. The mathematically equivalent matrix-vector product `resid @ nodes` avoids that broadcast temporary and can use the configured NumPy linear-algebra backend. Runtime gains depend on matrix shape, memory layout, BLAS implementation, and threading, so no universal percentage improvement should be claimed without a reproducible benchmark.
 **Action:** Prefer a matrix-vector product for equivalent quadrature-node reductions when dtype, shape, and numerical parity are preserved. Keep Rust as the primary production path, retain the NumPy implementation as a tested reference fallback, and benchmark representative workloads before making quantitative performance claims.
-## 2026-08-07 - Avoid N x J intermediate arrays in boolean mask reductions
-**Learning:** Element-wise multiplication of a full-size matrix with a boolean mask followed by an axis sum (e.g. `(v * observed).sum(axis=0)`) forces numpy to allocate a full intermediate array of the same shape. For metrics like `infit` in `fitstats.py`, this introduces measurable memory overhead when called inside hot loops or on large datasets.
-**Action:** Cast the boolean mask to the dtype of the data and use `np.einsum` to directly compute the reduction (e.g., `np.einsum('ij,ij->j', v, obs_f)`), skipping the intermediate array allocation entirely.
diff --git a/python/fast_mlsirm/fitstats.py b/python/fast_mlsirm/fitstats.py
--- a/python/fast_mlsirm/fitstats.py
+++ b/python/fast_mlsirm/fitstats.py
@@ -795,18 +795,17 @@ def infit_outfit(
         eta = eta - math.exp(params.tau) * dist
     p = np.clip(1.0 / (1.0 + np.exp(-np.clip(eta, -700, 700))), 1e-12, 1 - 1e-12)
     v = p * (1.0 - p)
-    resid2 = (y - p) ** 2 * observed
+    resid2 = np.subtract(y, p)
+    np.square(resid2, out=resid2)
+    np.multiply(resid2, observed, out=resid2)
     n_obs = np.maximum(observed.sum(axis=0), 1)
-
-    # Optimization: avoiding intermediate N x J array allocations.
-    # resid2 incorporates the observed mask since resid2 = (y - p)**2 * observed
-    # We use einsum to calculate (v * observed).sum(axis=0) skipping intermediate array allocation
-    obs_f = observed.astype(v.dtype, copy=False)
-
-    outfit = (resid2 / v).sum(axis=0) / n_obs
-    infit = resid2.sum(axis=0) / np.maximum(np.einsum('ij,ij->j', v, obs_f), 1e-12)
+    resid2_sum = resid2.sum(axis=0)
+    infit_denominator = np.sum(v, axis=0, where=observed)
+    np.divide(resid2, v, out=resid2)
+    outfit = resid2.sum(axis=0) / n_obs
+    infit = resid2_sum / np.maximum(infit_denominator, 1e-12)
     return {"infit": infit, "outfit": outfit}
 
 
