🎯 **What:**
- `python/fast_mlsirm/fit.py`의 `fit` 함수 내에 존재하던 최적화 루프 부분을 `_run_single_fit` 이라는 별도의 함수로 분리했습니다.
- `fit` 함수가 약 70줄에 달하는 긴 함수였는데, 내부 루프를 분리하여 가독성을 높였습니다.

💡 **Why:**
- 기존 `fit` 함수는 최적화 루프, 재시작 추적, 로깅 등을 모두 한 곳에서 처리하고 있어 가독성이 떨어졌습니다.
- 단일 최적화 실행 부분을 분리함으로써 각 함수의 책임이 명확해지고, 코드 유지보수성이 향상되었습니다.

✅ **Verification:**
- 코드 수정 후 구문 오류가 없음을 확인했습니다.
- 수정 전후 동작이 동일함을 `pytest tests/`를 통해 검증했습니다 (모든 테스트 통과).
- Rust 테스트(`cargo test`)를 통해 영향을 받는 부분이 없음을 재확인했습니다.
- `black`, `isort`, `ruff`를 통해 포맷팅 및 린트 검사를 완료했습니다.

✨ **Result:**
- `fit` 함수의 길이가 대폭 줄어들었으며 코드 의도가 명확해졌습니다.
- 기존의 동작을 전혀 변경하지 않고(Code Health Improvement) 내부 구조만을 개선했습니다.
