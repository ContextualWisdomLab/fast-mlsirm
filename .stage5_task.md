Stage 5 of fast-mlsirm #1912: GPU-parallel E-step for the merged polytomous bifactor GRM (stage 1, crates/mlsirm-core/src/bifactor_grm.rs) and multigroup bifactor (stage 2, PR #1926 merged), plus a joint person-bootstrap driver. Base origin/main, branch feat/1912-stage5-gpu.
Context: existing PR #1921 ("GPU-parallel polytomous bifactor, Lord-Wingersky recursion, and joint bootstrap") was rejected by the maintainer because it hard-coded a 390-replicate target. The ~390 figure is only a projection. In this library, replicate count, batch size, Monte Carlo stopping ratio (MC error of interval endpoints relative to half-width) and compute budget MUST be caller arguments with validated ranges and no defaults that encode study-specific numbers. Reuse whatever is correct in #1921's GPU kernels (check out its branch, read its review comments) rather than rewriting from scratch; close the loop by referencing #1921 in your PR.
Requirements: GPU path must be numerically equivalent to the CPU E-step (test: max abs diff of expected counts and loglik within a documented tolerance derived from float precision, on fixtures incl. reverse-keyed items); CPU fallback when no GPU; deterministic replicate seeding (seed argument) so CPU and GPU bootstraps match replicate-by-replicate; stopping rule implemented as in the cited source (e.g. Andrews & Buchinsky 2000 for bootstrap replicate-number selection, or Efron & Tibshirani 1993 ch. 19 — read the full text via Zotero http://localhost:23119 or KW library proxy https://<host-with-dashes>.libproxy.kw.ac.kr in system Edge; verified locators only). Report per-replicate convergence and never substitute failed replicates. Benchmarks recorded (CPU vs GPU wall time) in the PR, measured not estimated.
Rules: RED test first then GREEN; no unsourced caps/constants; non-force pushes; do not merge; report worker_done with PR URL.

Working directory: /Users/seonghobae/orca/workspaces/fast-mlsirm/feat-1912-stage5-gpu. Task file: .stage5_task.md (do not commit it). When finished, open the PR and post a PR comment starting with WORKER_DONE summarizing results and the PR head SHA.
