fix(ci): trigger retry for opencode-review timeout

This commit provides a trivial update to retry the opencode-review CI action.
The previous CI run timed out randomly due to an LLM provider context window issue.
No logic changes were necessary since the actual unit tests, rust tests, and packaging passed successfully.
