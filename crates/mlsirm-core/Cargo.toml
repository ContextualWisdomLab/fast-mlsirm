[package]
name = "mlsirm-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Core likelihood and gradient routines for fast-mlsirm."

[lib]
name = "mlsirm_core"
path = "src/lib.rs"
